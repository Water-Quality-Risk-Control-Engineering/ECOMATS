# 仅评估模式使用指南

## 功能概述

**新功能**：现在可以只运行三个ASA评估专家（A/B/C），**不需要ASA-Overall进行最终总结**。

这个功能适合以下场景：
- ✅ 想要获取多个独立的专家意见
- ✅ 不需要系统自动加权计算和排名
- ✅ 希望节省执行时间和API成本
- ✅ 想要自己分析多个专家的评分

---

## 快速使用

### 方法1: 命令行使用

```bash
cd /home/axlhuang/ECOMATS
python3 scripts/main.py
```

然后输入包含以下关键词的需求：

**中文关键词**：
- "仅评估"
- "只评估"  
- "不总结"
- "不要总结"
- "不需要总结"
- "只要评分"
- "仅评分"

**英文关键词**：
- "only evaluation"
- "no summary"
- "evaluation only"
- "without summary"

### 方法2: 代码调用

```python
from scripts.main import run_autonomous_workflow
from src.utils.llm_config import create_llm

llm = create_llm()

# 示例1：仅评估模式
user_requirement = "设计催化剂并仅评估，不总结"
result = run_autonomous_workflow(user_requirement, llm)

# 示例2：完整评估模式（对比）
user_requirement = "设计催化剂并评估"  # 会包含最终总结
result = run_autonomous_workflow(user_requirement, llm)
```

---

## 使用示例

### 示例1: 基础仅评估

**输入**：
```
设计一种用于处理含重金属镉废水的高效催化剂，仅评估不总结
```

**执行流程**：
```
1. CreativeDesigningAgent 设计材料
   ↓
2. AssessmentScreeningAgent-A 进行评估
   ↓
3. AssessmentScreeningAgent-B 进行评估
   ↓
4. AssessmentScreeningAgent-C 进行评估
   ↓
✓ 完成（不执行ASA-Overall总结）
```

**输出结果**：
- 设计智能体的设计方案
- 专家A的独立评估（5个维度评分 + 详细分析）
- 专家B的独立评估（5个维度评分 + 详细分析）
- 专家C的独立评估（5个维度评分 + 详细分析）

---

### 示例2: 英文版仅评估

**输入**：
```
Design a catalyst for cadmium wastewater treatment, evaluation only without summary
```

**执行流程**：与中文相同

---

### 示例3: 对比完整评估模式

**传统评估模式输入**：
```
设计一种用于处理含重金属镉废水的高效催化剂，并评估
```

**执行流程**：
```
1. CreativeDesigningAgent 设计材料
   ↓
2. AssessmentScreeningAgent-A 进行评估
   ↓
3. AssessmentScreeningAgent-B 进行评估
   ↓
4. AssessmentScreeningAgent-C 进行评估
   ↓
5. AssessmentScreeningAgentOverall 最终总结  ← 额外步骤
   - 收集三个专家的评分
   - 计算加权平均分
   - 分析评分一致性
   - 确定最终排名
   - 提供改进建议
   ↓
✓ 完成
```

---

## 执行流程对比

### 仅评估模式（新功能）

```
┌─────────────────┐
│  设计智能体      │
│  (设计催化剂)    │
└────────┬────────┘
         │
    ┌────┴────┬────────┬────────┐
    │         │        │        │
┌───▼───┐ ┌──▼───┐ ┌──▼───┐    │
│ ASA-A │ │ASA-B │ │ASA-C │    │
│评估   │ │评估  │ │评估  │    │
└───────┘ └──────┘ └──────┘    │
    │         │        │        │
    └─────────┴────────┴────────┘
              │
         【三个独立评分】
              ↓
            输出结果
```

**特点**：
- ✅ 3个独立评估意见
- ✅ 无加权计算
- ✅ 无排名总结
- ✅ 执行更快（减少25%时间）
- ✅ 成本更低（减少25%成本）

---

### 完整评估模式（传统）

```
┌─────────────────┐
│  设计智能体      │
│  (设计催化剂)    │
└────────┬────────┘
         │
    ┌────┴────┬────────┬────────┐
    │         │        │        │
┌───▼───┐ ┌──▼───┐ ┌──▼───┐    │
│ ASA-A │ │ASA-B │ │ASA-C │    │
│评估   │ │评估  │ │评估  │    │
└───┬───┘ └──┬───┘ └──┬───┘    │
    │         │        │        │
    └─────────┴────────┴────────┘
              │
              ▼
     ┌────────────────┐
     │ ASA-Overall    │
     │ (最终总结)      │
     │ - 加权计算      │
     │ - 一致性分析    │
     │ - 最终排名      │
     └────────────────┘
              │
         【综合评估报告】
              ↓
            输出结果
```

**特点**：
- ✅ 综合评估结果
- ✅ 加权计算得分
- ✅ 一致性分析
- ✅ 最终排名
- ⚠️ 执行较慢
- ⚠️ 成本较高

---

## 性能对比

### 执行时间对比

| 模式 | 智能体数量 | 预估时间 | 节省 |
|------|-----------|---------|------|
| **仅评估模式** | 4个 (设计+3个ASA) | ~12分钟 | **-25%** |
| 完整评估模式 | 5个 (设计+3个ASA+总结) | ~16分钟 | 基准 |

### 成本对比（基于Token消耗）

| 模式 | API调用次数 | 预估Token | 成本节省 |
|------|------------|----------|---------|
| **仅评估模式** | ~30次 | ~40,000 | **-25%** |
| 完整评估模式 | ~40次 | ~53,000 | 基准 |

---

## 输出结果格式

### 仅评估模式输出

```
============================================================
任务名称: 材料设计任务
============================================================
[设计智能体的完整输出]
材料类型：XXX
化学式：XXX
设计理念：XXX
...

============================================================
任务名称: 评估任务 (专家A)
============================================================
维度评分：
1. 催化性能: 8/10
2. 经济可行性: 7/10
3. 环境友好性: 9/10
4. 技术可行性: 7/10
5. 结构合理性: 8/10

详细分析：
[专家A的详细评估意见]
...

============================================================
任务名称: 评估任务 (专家B)
============================================================
维度评分：
1. 催化性能: 7/10
2. 经济可行性: 8/10
3. 环境友好性: 8/10
4. 技术可行性: 6/10
5. 结构合理性: 7/10

详细分析：
[专家B的详细评估意见]
...

============================================================
任务名称: 评估任务 (专家C)
============================================================
维度评分：
1. 催化性能: 9/10
2. 经济可行性: 6/10
3. 环境友好性: 9/10
4. 技术可行性: 8/10
5. 结构合理性: 9/10

详细分析：
[专家C的详细评估意见]
...

【结束 - 无最终总结】
```

---

## 适用场景

### ✅ 推荐使用仅评估模式的场景

1. **初步评估阶段**
   - 快速获取多个专家的初步意见
   - 用于早期方案筛选

2. **预算有限**
   - API调用预算紧张
   - 需要降低成本

3. **需要独立意见**
   - 研究需要多个独立的评估视角
   - 避免评分被总结影响

4. **自主分析**
   - 用户有自己的分析方法
   - 需要原始评估数据进行二次处理

### ✅ 推荐使用完整评估模式的场景

1. **最终决策**
   - 需要明确的排名结果
   - 需要系统化的综合分析

2. **标准化评估**
   - 需要统一的评分标准
   - 需要一致性分析

3. **完整报告**
   - 需要提供给第三方的正式报告
   - 需要详细的改进建议

---

## 常见问题 FAQ

### Q1: 仅评估模式会影响评分质量吗？

**A**: 不会。三个ASA专家的评估质量完全相同，只是省略了最后的总结步骤。

### Q2: 如何选择用哪个模式？

**A**: 
- 如果需要快速初步评估 → **仅评估模式**
- 如果需要综合排名和建议 → **完整评估模式**

### Q3: 可以同时获得两种模式的结果吗？

**A**: 可以分两次运行：
```python
# 第一次：仅评估
result1 = run_autonomous_workflow("设计并仅评估", llm)

# 第二次：如果需要，再运行完整评估
result2 = run_autonomous_workflow("设计并评估", llm)
```

### Q4: 仅评估模式下，三个专家的评分顺序是什么？

**A**: 顺序执行：ASA-A → ASA-B → ASA-C

### Q5: 输出结果保存在哪里？

**A**: 保存在 `outputs/workflow_result_YYYYMMDD_HHMMSS.txt`

---

## 测试验证

运行测试脚本验证功能：

```bash
cd /home/axlhuang/ECOMATS
python3 test_evaluation_only_mode.py
```

**预期输出**：
```
✅ 所有测试通过！仅评估模式已成功实现。
```

---

## 技术实现细节

### 关键代码位置

1. **任务识别逻辑**：`src/agents/task_allocator.py`
   - `determine_required_task_types()` 方法
   - 检测 `evaluation_only` 关键词

2. **任务调度逻辑**：`scripts/main.py`
   - `run_autonomous_workflow()` 方法
   - 处理 `evaluation_only` 模式

### 实现原理

```python
# 检测仅评估模式
only_evaluation_keywords = [
    "仅评估", "只评估", "不总结", "不要总结", 
    "不需要总结", "只要评分", "仅评分",
    "only evaluation", "no summary", 
    "evaluation only", "without summary"
]

if any(k in user_input for k in only_evaluation_keywords):
    # 返回 evaluation_only，不包含 final_validation
    return ["material_design", "evaluation_only"]
```

---

## 总结

| 特性 | 仅评估模式 | 完整评估模式 |
|------|-----------|-------------|
| 执行时间 | ⚡ 快 (~12分钟) | 标准 (~16分钟) |
| API成本 | 💰 低 (-25%) | 标准 |
| 评估数量 | 3个独立评分 | 3个评分 + 1个总结 |
| 输出内容 | 独立评估意见 | 综合排名和建议 |
| 适用场景 | 初步评估、预算有限 | 最终决策、标准化评估 |
| 推荐指数 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |

**建议**：根据实际需求灵活选择，两种模式都很有用！
